{
  "name": "local-ai-classifier",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "license": "MIT",
  "description": "PoC harness for trying local GGUF models and system prompts on offline text classification (macOS)",
  "scripts": {
    "prebuild": "bun run scripts/download-llama.ts",
    "build": "bun build --compile --target bun --outfile sailkari src/index.ts",
    "dist": "mkdir -p sailkari-dist && cp sailkari sailkari-dist/ && cp -a bin sailkari-dist/",
    "start": "bun run src/index.ts",
    "test": "bun test src",
    "test:e2e": "bun test e2e"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
